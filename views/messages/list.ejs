<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages - Members Only Club</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .glow-effect {
            box-shadow: 0 0 20px rgba(56, 189, 248, 0.3);
        }

        .glow-effect:hover {
            box-shadow: 0 0 30px rgba(56, 189, 248, 0.5);
        }

        .glassmorphism {
            backdrop-filter: blur(10px);
            background-color: rgba(30, 41, 59, 0.8);
        }
    </style>
</head>

<body class="min-h-screen" style="background-color: #0F172A;">
    <nav class="p-5 shadow-lg glassmorphism" style="color: #E2E8F0;">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">📬 All Messages</h1>
            <div class="flex gap-5">
                <a href="/" class="px-4 py-2 rounded-lg transition-colors" style="color: #E2E8F0;"
                    onmouseover="this.style.backgroundColor='rgba(56, 189, 248, 0.2)'"
                    onmouseout="this.style.backgroundColor='transparent'">Home</a>
                <% if (user) { %>
                    <a href="/profile" class="px-4 py-2 rounded-lg transition-colors" style="color: #E2E8F0;"
                        onmouseover="this.style.backgroundColor='rgba(56, 189, 248, 0.2)'"
                        onmouseout="this.style.backgroundColor='transparent'">Profile</a>
                    <% if (user.is_admin) { %>
                        <a href="/admin" class="px-4 py-2 rounded-lg transition-colors" style="color: #E2E8F0;"
                            onmouseover="this.style.backgroundColor='rgba(56, 189, 248, 0.2)'"
                            onmouseout="this.style.backgroundColor='transparent'">Admin</a>
                        <% } %>
                            <a href="/logout" class="px-4 py-2 rounded-lg transition-colors" style="color: #E2E8F0;"
                                onmouseover="this.style.backgroundColor='rgba(56, 189, 248, 0.2)'"
                                onmouseout="this.style.backgroundColor='transparent'">Logout</a>
                            <% } else { %>
                                <a href="/login" class="px-4 py-2 rounded-lg transition-colors" style="color: #E2E8F0;"
                                    onmouseover="this.style.backgroundColor='rgba(56, 189, 248, 0.2)'"
                                    onmouseout="this.style.backgroundColor='transparent'">Login</a>
                                <a href="/signup" class="px-4 py-2 rounded-lg transition-colors" style="color: #E2E8F0;"
                                    onmouseover="this.style.backgroundColor='rgba(56, 189, 248, 0.2)'"
                                    onmouseout="this.style.backgroundColor='transparent'">Sign
                                    Up</a>
                                <% } %>
            </div>
        </div>
    </nav>

    <div class="max-w-6xl mx-auto my-10 px-5">
        <div class="flex justify-between items-center mb-8">
            <h2 class="text-2xl font-bold" style="color: #E2E8F0;">Community Messages</h2>
            <% if (user) { %>
                <a href="/messages/new"
                    class="px-6 py-3 rounded-lg font-semibold hover:-translate-y-0.5 transition-all glow-effect"
                    style="background-color: #38BDF8; color: #0F172A;">✏️
                    Create Message</a>
                <% } %>
        </div>

        <% if (messages && messages.length> 0) { %>
            <div class="grid gap-5">
                <% messages.forEach(message=> { %>
                    <div class="rounded-lg p-6 shadow-md hover:-translate-y-0.5 hover:shadow-xl transition-all"
                        style="background-color: #1E293B;">
                        <h3 class="text-xl font-bold mb-3" style="color: #E2E8F0;">
                            <%= message.title %>
                        </h3>
                        <p class="leading-relaxed mb-4" style="color: #94A3B8;">
                            <%= message.text %>
                        </p>
                        <div class="flex justify-between items-center pt-4 border-t text-sm"
                            style="border-color: #334155;">
                            <div style="color: #94A3B8;">
                                <% if (user && user.is_member && message.first_name) { %>
                                    <span class="font-semibold" style="color: #38BDF8;">👤 <%= message.first_name %>
                                            <%= message.last_name %></span>
                                    <span class="mx-2">•</span>
                                    <span>
                                        <%= new Date(message.created_at).toLocaleDateString('en-US', { month: 'short' ,
                                            day: 'numeric' , year: 'numeric' }) %>
                                    </span>
                                    <% } else { %>
                                        <span class="italic">🔒 Anonymous (Join the club to see authors)</span>
                                        <% } %>
                            </div>
                            <% if (user && user.is_admin) { %>
                                <form action="/messages/<%= message.message_id %>/delete" method="POST" class="inline"
                                    onsubmit="return confirm('Are you sure you want to delete this message?')">
                                    <button type="submit" class="px-4 py-2 rounded-lg text-sm transition-colors"
                                        style="background-color: #DC2626; color: #E2E8F0;"
                                        onmouseover="this.style.backgroundColor='#B91C1C'"
                                        onmouseout="this.style.backgroundColor='#DC2626'">🗑️
                                        Delete</button>
                                </form>
                                <% } %>
                        </div>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <div class="text-center py-20" style="color: #64748B;">
                    <h2 class="text-2xl font-bold mb-2">No messages yet</h2>
                    <p>Be the first to share your thoughts!</p>
                </div>
                <% } %>
    </div>
</body>

</html>